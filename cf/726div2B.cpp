#include <bits/stdc++.h>

#define ll long long

using namespace std;

int t;
int main()
{
    ll n, m, x, y;
    cin >> t;
    while (t--)
    {
        cin >> n >> m >> x >> y;
        cout << 1 << " " << 1 << " " << n << " " << m << endl;
    }
}
